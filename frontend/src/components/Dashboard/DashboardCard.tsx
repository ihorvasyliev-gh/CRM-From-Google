/** Dashboard widgets use the shared card surface; kept as a named module for existing imports. */
export { default } from '../ui/Card';
export type { CardProps as DashboardCardProps } from '../ui/Card';
